{# Intro content #}
{% set introHeading = title %}
{% set footerSummary = site.footerSummary %}
{% set metaDescription = description if description else 
  site.shortDesc %}

<!DOCTYPE html>
<html lang="en" class="no-js">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="alternate" type="application/rss+xml" title="{{ site.title }}" href="{{ site.url }}/feed.xml"/>

    <link rel="preconnect" href="https://fonts.googleapis.com"/>
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:ital,wght@0,300;0,400;0,500;1,300;1,400&display=swap"
      rel="stylesheet"
/>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css">

    {% if site.theme %}
      <style>

        :root {
          --THEME_BACKGROUND: {{site.theme.primary.background}};
          --THEME_COLOR: {{site.theme.primary.text}};
          --THEME_HIGHLIGHT: {{site.theme.primary.highlight}};
          --THEME_HOVER: #404040;
          --THEME_UNDERLINE: #C8D5F7;
          --COLOR_DARK: #333;
          --COLOR_LIGHTER: #eaeaea;
          --COLOR_MID: #767676;
        }

        @media (prefers-color-scheme: dark) {
          :root {
            --THEME_BACKGROUND: #0a0a0a;
            --THEME_COLOR: #fff;
            --THEME_HIGHLIGHT: #fff;
            --THEME_HOVER: #e8e8e8;
            --COLOR_DARK: #dedede;
            --COLOR_LIGHTER: #000;
            --COLOR_MID: #c1c1c1;
          }
        }

        .theme-secondary {
          --THEME_BACKGROUND: {{site.theme.secondary.background}};
          --THEME_COLOR: #404040;
          --THEME_HIGHLIGHT: #000;
        }

        @media (prefers-color-scheme: dark) {
          .theme-secondary {
            --THEME_BACKGROUND: {{site.theme.secondary.background}};
            --THEME_COLOR: #e8e8e8;
            --THEME_HIGHLIGHT: #fff;
          }
        }
      </style>
    {% endif %}

    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/prism-themes@1.9.0/themes/prism-one-light.min.css" media="(prefers-color-scheme: light)">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/prism-themes@1.9.0/themes/prism-dracula.min.css" media="(prefers-color-scheme: dark)">

    {% if extra_css %}
      {% for item in extra_css %}
        <link rel="stylesheet" href="{{item}}">
      {% endfor %}
    {% endif %}

    {% include "partials/services/keystone-login-styles.njk" %}

    {% include "partials/global/meta-info.njk" %}
    {% block head %}{% endblock %}

  </head>
  <body>
    <a class="visually-hidden skip-content" href="#main-content">Skip to content</a>
    <div class="l-page">
      {% include "partials/global/site-head.njk" %}
      <main id="main-content" tabindex="-1">
        <div class="l-stack l-stack--large">
          {% block content %}{% endblock content %}
        </div>
      </main>
    </div>
    {% block foot %}
      {% include "partials/global/site-foot.njk" %}
    {% endblock %}
    <script type="text/javascript" src="/js/service-worker.js"></script>
    <script type="text/javascript" src="/js/main.js"></script>
    {% include "partials/services/keystone-login-scripts.njk" %}
    <script type="module">
      import { inject } from '@vercel/analytics';
      inject();
    </script>
    {% if extra_js %}
      {% for item in extra_js %}
        <script type="text/javascript" src="{{item}}"></script>
      {% endfor %}
    {% endif %}
    {% mermaid_js %}
    <script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.js"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/contrib/auto-render.min.js" onload="renderMathInElement(document.body, {delimiters: [{left: '$$', right: '$$', display: true}, {left: '$', right: '$', display: false}]});"></script>
  </body>
</html>